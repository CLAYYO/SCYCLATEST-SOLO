# Supabase Configuration
# Get these values from your Supabase project dashboard

# Public Supabase URL - safe to expose in client-side code
PUBLIC_SUPABASE_URL=your_supabase_project_url_here

# Public Supabase Anon Key - safe to expose in client-side code
PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase Service Role Key - KEEP SECRET! Only use server-side
# This key has elevated privileges and should never be exposed in client code
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual Supabase credentials
# 3. Never commit the .env file to version control
# 4. The SERVICE_ROLE_KEY should only be used in server-side scripts and API routes